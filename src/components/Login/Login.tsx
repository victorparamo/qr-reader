import { Container, Card } from './styles';

const Login = (): JSX.Element => {
  // console.log('login');

  return (
    <Container>
      <Card elevation={12}>Login</Card>
    </Container>
  );
};

export default Login;
